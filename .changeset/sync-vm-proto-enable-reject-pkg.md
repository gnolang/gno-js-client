---
"@gnolang/gno-js-client": minor
---

Sync `vm.proto` with `gnolang/gno` master: add `MsgEnablePackage` (`/vm.m_enable_pkg`) and `MsgRejectPackage` (`/vm.m_reject_pkg`) message types, export them from the proto index, and add the matching `MsgEndpoint` entries.
